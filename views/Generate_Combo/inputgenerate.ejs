<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Input Generate</title>
</head>
<body>
    <div>
        <%if (type === "radio" || type === "checkbox") { %>
            <%-input %>
        <% } else { %>
            <%-select%>
        <% } %>
    </div>   
</body>
</html>